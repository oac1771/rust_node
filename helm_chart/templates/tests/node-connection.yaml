apiVersion: v1
kind: Pod
metadata:
  name: test-node-connection
  annotations:
    "helm.sh/hook": test
spec:
  securityContext:
    runAsUser: 0
  containers:
    - name: test-node
      image: docker.io/curlimages/curl
      imagePullPolicy: IfNotPresent
      command: ['sh', '-c']
      args:
        - apk add jq && curl http://{{ .Values.node.containerName }}.{{ .Values.node.namespace}}:{{ .Values.node.apiPort}}/health | jq ".status_code" -e
  restartPolicy: Never